# 蓝绿松石版 · 前端重构开发说明

本项目是「社系毕业晚会解密网站」的**独立副本**，专用于按「蓝绿松石」设计稿重构前端。
与原项目完全分开，互不影响。

## 文件布局

- `设计稿参考_蓝绿松石.html` —— **视觉目标**。自包含静态稿（内联样式 + 假数据，不连后端）。
  双击即可在浏览器查看要做成的样子。重构时照着它来。
- `static/` —— **真正要改的前端**（这是后端实际伺服的内容）：
  - `app.js`（页面渲染逻辑）
  - `styles.css`（配色与样式）
  - `index.html`（外壳）
  - `assets/`（信箱照、简谱图等真实素材）
- `app.py` —— 后端服务器。**纯视觉重构通常不需要改它**；要保持与后端的所有交互不变。
- `data/` —— 运行时自动生成的本机数据库（已被 git 忽略）。

## 重构目标

把 `static/` 里的 `app.js` + `styles.css`（必要时 `index.html`）改写成
`设计稿参考_蓝绿松石.html` 的视觉效果，**同时保持所有答题/抽奖/后台交互和后端数据流不变**。

## 怎么运行

```bash
cd /Users/lynn/Desktop/技术力命题法_蓝绿松石版
PORT=8001 python3 app.py
```

浏览器打开 `http://127.0.0.1:8001`。（用 8001 是为了能和原版的 8000 同时开着对比。）

## 安全网

- **原版**在 `/Users/lynn/Desktop/技术力命题法/`，原封未动，随时用 `python3 app.py`（8000 端口）启动对照。
- 本项目有自己的 git。基线提交是「init: 蓝绿松石版重构项目基线」。
  改坏了想回到起点：`git restore .`（或 `git checkout .`）。
  建议每完成一屏/一块就 `git commit` 存一次档。
